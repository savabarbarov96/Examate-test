# JWT Secrets (IMPORTANT: Generate long random strings for production)
# Generate with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
JWT_REFRESH_SECRET=your-super-secret-refresh-key-minimum-32-characters-long

# Email Configuration (SMTP)
EMAIL_USERNAME=noreply@example.com
EMAIL_PASSWORD=your-email-password

# Frontend API URLs (used during the Vite build)
VITE_API_URL=http://localhost:5001
VITE_AUTH_API_URL=http://localhost:5000

# Database Credentials (Optional - defaults are in docker-compose.yml)
# MONGO_ROOT_USERNAME=root
# MONGO_ROOT_PASSWORD=examate_mongo_pass
